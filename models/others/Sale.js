class Sale {
    constructor(docId, fullName, price, type, isSold) {
       this.docId = docId;
       this.fullName = fullName;
       this.price = price;
       this.type = type;
       this.isSold = isSold;
      }
}

export default Sale;